import { PersonalizarFechaPipe } from './personalizar-fecha.pipe';

describe('PersonalizarFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalizarFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
